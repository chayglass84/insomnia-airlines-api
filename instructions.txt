INSOMNIA AIRLINES API — HOW TO RUN THIS THING
==============================================

TECH STACK
----------
Python         The programming language. You likely already have it.
FastAPI        A Python library that turns your code into a web API.
               It also auto-generates interactive API docs for free.
Uvicorn        A small web server that runs the FastAPI app locally.
               Think of it like "the thing that listens on a port."

All the fake data (airports, routes, flights) lives in memory inside
main.py. Nothing is written to disk. Restarting the server resets
everything back to the seed data — that's intentional for now.


STARTING THE SERVER
-------------------
1. Open Terminal
2. Navigate to this folder:
      cd ~/projects/insomnia-airlines-api

3. Start the server:
      /usr/bin/python3 -m uvicorn main:app --reload

   The "--reload" flag means the server restarts automatically whenever
   you edit main.py. Great for development, leave it out in production.

   NOTE: On Kong-managed Macs, the `python3` shell command is wrapped by
   a corporate certificate tool and will fail with a permissions error.
   Use the full path /usr/bin/python3 instead.

4. You'll see something like:
      INFO:     Uvicorn running on http://127.0.0.1:8000

5. Open your browser to:
      http://localhost:8000         ← the simple endpoint reference page
      http://localhost:8000/docs    ← interactive docs (try requests here!)
      http://localhost:8000/redoc   ← alternative docs view

6. To stop the server: press Ctrl+C in Terminal.


INSTALLING DEPENDENCIES (first time only)
------------------------------------------
If you get "ModuleNotFoundError" when starting, run this first:
      /usr/bin/pip3 install -r requirements.txt

Then try starting the server again.


COMMON ERRORS
-------------
"zsh: command not found: uvicorn"
   Don't use `uvicorn` by itself. Always use:
      /usr/bin/python3 -m uvicorn main:app --reload

"ModuleNotFoundError: No module named 'fastapi'"
   Run:  /usr/bin/pip3 install -r requirements.txt

"Address already in use"
   Something is already running on port 8000.
   Either stop that thing, or use a different port:
      /usr/bin/python3 -m uvicorn main:app --reload --port 8001
   Then visit http://localhost:8001 instead.

"command not found: python3" or permissions error mentioning ca-key.pem
   Your python3 shell command is wrapped by a corporate certificate tool.
   Use /usr/bin/python3 explicitly — it bypasses the wrapper.


THE DATA
--------
All fake data is hardcoded near the top of main.py in these dictionaries:

   AIRPORTS             10 North American airports (YYZ, SFO, ORD, etc.)
   ROUTES               10 city-pair routes between those airports
   FLIGHTS              10 scheduled flights on 2026-05-08 and 2026-05-09
   FREQUENT_FLYERS      2 profiles: FF1029384 (Alex Morgan), FF2938475 (Sam Taylor)
   BOOKINGS             5 bookings with PNRs: AB12CD, EF34GH, IJ56KL, MN78OP, QR90ST
   SEAT_ASSIGNMENTS     3 seat assignments across those bookings
   BAGS                 6 bags linked to real PNRs and flights
   BAG_EVENTS           Tracking history for each bag (check-in, load, claim, etc.)
   LOST_BAGGAGE_CASES   2 cases: one SEARCHING (Lisa Park's roller bag), one OPEN

To change the data, edit those dictionaries directly in main.py.
The server will reload automatically if you started it with --reload.

KEY IDs TO KNOW (useful when calling endpoints manually)
---------------------------------------------------------
PNRs (booking codes):
   AB12CD  — Alex Morgan + Sam Taylor, IA204 YYZ→ORD, CONFIRMED
   EF34GH  — Jordan Lee, IA101 YYZ→SFO business class, CONFIRMED
   IJ56KL  — Maria Santos, IA933 YYZ→SFO (tomorrow), HELD
   MN78OP  — David Chen, IA530 YUL→FLL, FLOWN
   QR90ST  — Lisa Park, IA315 YYZ→IAD, CANCELLED

Frequent flyer IDs:
   FF1029384  Alex Morgan (GOLD, 48,230 miles)
   FF2938475  Sam Taylor (SILVER, 12,500 miles)

Bag tags (10-digit numbers):
   0074123456  Alex Morgan's bag, LOADED on IA204
   0074123457  Sam Taylor's bag, LOADED on IA204
   0074234567  Jordan Lee's bag, LOADED on IA101
   0074345678  David Chen's bag, CLAIMED at FLL
   0074456789  Maria Santos's bag, CHECKED_IN for tomorrow
   0074567890  Lisa Park's bag, LOST at ORD

Lost baggage case IDs:
   c1000001-0000-0000-0000-000000000000  Lisa Park's roller bag, SEARCHING
   c1000002-0000-0000-0000-000000000000  David Chen's duffel, OPEN


DEPLOYING TO RENDER (free tier)
--------------------------------
1. Go to https://render.com and sign in (or sign up — it's free)
2. Click "New" → "Web Service"
3. Connect your GitHub account and select the insomnia-airlines-api repo
4. Render will detect render.yaml automatically — just click Deploy
5. Your API will be live at a URL like:
      https://insomnia-airlines-api.onrender.com

Notes:
- The free tier spins down after 15 minutes of inactivity. The first
  request after a gap takes ~30 seconds to wake up. That's normal.
- Data resets to seed data every time the service restarts or wakes up
  (same as local — it's all in memory).
- The local start command uses --reload and port 8000. Render uses its
  own $PORT variable and no --reload. render.yaml handles this for you.


THE FILES
---------
main.py          The entire API — all endpoints and all data live here
index.html       The reference page shown at localhost:8000 (includes demo URLs)
requirements.txt The list of libraries this project needs
flights.yaml     OpenAPI spec for airports, routes, and flights
bookings.yaml    OpenAPI spec for bookings, passengers, and seats
baggage.yaml     OpenAPI spec for bags, tracking events, and lost baggage
security.yaml    OpenAPI spec for auth token and test data endpoints
instructions.txt This file
